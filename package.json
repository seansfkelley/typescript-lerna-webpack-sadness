{
  "name": "typescript-lerna-sadness",
  "version": "1.0.0",
  "scripts": {
    "start": "lerna bootstrap && cd packages/parent && npm run tsc && npm run webpack"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.29"
  }
}
